# toImports

```ts
function Sources.toImports(sources: Source[]): Source[]
```

Takes a list of sources and returns a clean string containing import declarations for each one, based on [`Source.toImport`](../Source/toImport).
